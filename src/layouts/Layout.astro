---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  canonicalPath?: string;
}

const {
  title = 'Stellart Corp — Agentes IA y Automatización Empresarial en Villavicencio, Colombia',
  description = 'Construimos agentes IA, software a medida y sistemas de decisión en tiempo real. Automatización empresarial para Colombia desde Villavicencio, Llanos Orientales.',
  canonicalPath,
} = Astro.props;

const pathname = Astro.url.pathname;
const canonicalURL = new URL(canonicalPath ?? pathname, 'https://www.stellart.site').href;

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "name": "Stellart Corp",
  "url": "https://www.stellart.site",
  "telephone": "+573027470798",
  "email": "hola@stellart.site",
  "description": "Agentes IA Villavicencio. Automatización empresarial Colombia. Sistemas de decisión con datos desde los Llanos Orientales para el mundo.",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Cl. 38 #No. 30 A - 60",
    "addressLocality": "Villavicencio",
    "addressRegion": "Meta",
    "addressCountry": "CO"
  },
  "areaServed": ["Colombia", "Latinoamérica"],
  "knowsAbout": ["Agentes IA", "Automatización empresarial", "Inteligencia artificial", "Software a medida", "Dashboards KPIs"]
};
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Stellart Corp" />
    <meta property="og:url" content={canonicalURL} />
    <title>{title}</title>
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
    <link rel="icon" type="image/svg+xml" href="/lyon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" sizes="any" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,600;0,700;1,600;1,700&family=DM+Sans:wght@300;400;500&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>

    <div class="noise" aria-hidden="true">
      <svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
        <filter id="noise-filter">
          <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch" />
        </filter>
        <rect width="100%" height="100%" filter="url(#noise-filter)" />
      </svg>
    </div>

    <nav>
      <div class="container nav-inner">
        <a href="/" class="logo">
          <span class="logo-mark">
            <span class="logo-pulse"></span>
          </span>
          <span class="logo-text">Stellart Corp</span>
        </a>
        <ul class="nav-links">
          <li><a href="/" class={pathname === '/' ? 'active' : ''}>Inicio</a></li>
          <li><a href="/servicios" class={pathname === '/servicios' ? 'active' : ''}>Soluciones</a></li>
          <li><a href="/agentes-ia-villavicencio" class={pathname === '/agentes-ia-villavicencio' ? 'active' : ''}>Agentes IA</a></li>
          <li><a href="/id" class={pathname === '/id' ? 'active' : ''}>I+D</a></li>
          <li><a href="/contacto" class={pathname === '/contacto' ? 'active' : ''}>Contacto</a></li>
        </ul>
        <a href="/contacto" class="btn btn-primary nav-cta">Agenda tu diagnóstico gratis →</a>
      </div>
    </nav>

    <main>
      <slot />
    </main>

    <footer>
      <div class="container">
        <div class="footer-grid">
          <div class="footer-brand">
            <a href="/" class="logo">
              <span class="logo-mark">
                <span class="logo-pulse"></span>
              </span>
              <span class="logo-text">Stellart Corp</span>
            </a>
            <p class="footer-tagline">Inteligencia artificial operacional desde Villavicencio, Colombia.</p>
          </div>
          <div class="footer-links">
            <div class="footer-col">
              <span class="footer-col-title">Navegación</span>
              <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/servicios">Soluciones</a></li>
                <li><a href="/agentes-ia-villavicencio">Agentes IA</a></li>
                <li><a href="/id">I+D</a></li>
                <li><a href="/contacto">Contacto</a></li>
              </ul>
            </div>
            <div class="footer-col">
              <span class="footer-col-title">Soluciones</span>
              <ul>
                <li><a href="/servicios#diagnostico">Diagnóstico</a></li>
                <li><a href="/servicios#ia">Agentes IA</a></li>
                <li><a href="/servicios#software">Software a medida</a></li>
                <li><a href="/servicios#marketing">Marketing con IA</a></li>
              </ul>
            </div>
            <div class="footer-col">
              <span class="footer-col-title">Contacto</span>
              <ul>
                <li><a href="mailto:hola@stellart.site">hola@stellart.site</a></li>
                <li><a href="tel:+573027470798">+57 302 747 0798</a></li>
                <li><a href="/contacto">Agenda diagnóstico gratis</a></li>
              </ul>
            </div>
          </div>
        </div>
        <div class="footer-bottom">
          <span>Stellart Corp © — Villavicencio, Meta, Colombia</span>
          <span class="footer-meta">Construido desde el Llano para el mundo.</span>
        </div>
      </div>
    </footer>

    <script>
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('revealed');
            }
          });
        },
        { threshold: 0.08, rootMargin: '0px 0px -48px 0px' }
      );

      document.querySelectorAll('.scroll-reveal').forEach((el) => {
        observer.observe(el);
      });
    </script>

  </body>
</html>

<style>
  nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    background: rgba(9, 8, 15, 0.65);
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
  }

  .nav-inner {
    display: flex;
    align-items: center;
    height: 66px;
    gap: 32px;
  }

  .logo {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    flex-shrink: 0;
  }

  .logo-mark {
    width: 26px;
    height: 26px;
    background: var(--accent);
    border-radius: 50%;
    position: relative;
    flex-shrink: 0;
  }

  .logo-mark::after {
    content: '';
    position: absolute;
    width: 7px;
    height: 7px;
    background: #000;
    border-radius: 50%;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .logo-pulse {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    border: 2px solid var(--accent);
    animation: logoPulse 2.4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    opacity: 0;
  }

  @keyframes logoPulse {
    0%   { transform: scale(1);   opacity: 0.7; }
    100% { transform: scale(2.2); opacity: 0;   }
  }

  .logo-text {
    font-family: var(--font-display);
    font-size: 20px;
    color: var(--text-primary);
    font-weight: 700;
    letter-spacing: -0.01em;
  }

  .nav-links {
    display: flex;
    list-style: none;
    gap: 28px;
    margin-left: auto;
  }

  .nav-links a {
    font-family: var(--font-sans);
    font-size: 14px;
    font-weight: 400;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s;
    position: relative;
    padding-bottom: 2px;
  }

  .nav-links a:hover {
    color: var(--text-primary);
  }

  .nav-links a.active {
    color: var(--text-primary);
  }

  .nav-links a.active::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    right: 0;
    height: 1px;
    background: var(--accent);
    box-shadow: 0 0 6px var(--accent-glow);
  }

  .nav-cta {
    padding: 9px 18px;
    font-size: 13px;
    flex-shrink: 0;
    margin-left: 8px;
  }

  footer {
    border-top: 1px solid var(--border-subtle);
    padding: 64px 0 40px;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 64px;
    margin-bottom: 48px;
    padding-bottom: 48px;
    border-bottom: 1px solid var(--border-subtle);
  }

  .footer-tagline {
    margin-top: 14px;
    font-size: 14px;
    color: var(--text-muted);
    font-weight: 300;
    max-width: 220px;
    line-height: 1.6;
  }

  .footer-links {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .footer-col-title {
    font-family: var(--font-mono);
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--text-muted);
    display: block;
    margin-bottom: 16px;
  }

  .footer-col ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .footer-col a {
    font-size: 14px;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s;
    font-weight: 300;
  }

  .footer-col a:hover {
    color: var(--text-primary);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--text-muted);
  }

  @media (max-width: 1024px) {
    .footer-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }
  }

  @media (max-width: 768px) {
    .nav-links {
      display: none;
    }

    .nav-cta {
      margin-left: auto;
    }

    .footer-links {
      grid-template-columns: repeat(2, 1fr);
    }

    .footer-bottom {
      flex-direction: column;
      gap: 8px;
      text-align: center;
    }
  }
</style>
